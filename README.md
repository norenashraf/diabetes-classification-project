# Diabetes Diagnosis Classification

An end-to-end machine learning project that classifies patients into **Non-Diabetic (N)**, **Pre-Diabetic (P)**, or **Diabetic (Y)** based on demographic and lab-test data, with a deployed Streamlit prediction app.

## Overview

| | |
|---|---|
| **Task** | Multi-class classification |
| **Target** | `CLASS` — N / P / Y |
| **Records** | 1,000 raw → 826 after cleaning |
| **Features** | 11 (demographics, lipid/kidney labs, HbA1c, BMI) |
| **Best model** | Tuned Random Forest — **98% accuracy**, **94% F1-macro** on held-out test set |

## Project Structure

```
├── Dataset_of_Diabetes_.csv                    # Raw dataset
├── diabetes_data_cleaned.csv                   # Cleaned dataset (generated by the notebook)
├── Diabetes_Classification_Full_Project.ipynb   # Full pipeline: cleaning → EDA → modeling
├── Diabetes_Prediction_deployment.py            # Streamlit app
├── final_diabetes_classification_model.pkl      # Trained model pipeline (generated)
├── label_encoder.pkl                            # Target label encoder (generated)
├── requirements.txt
└── README.md
```

## Pipeline

1. **Data Cleaning** — dropped identifier columns, removed duplicates, checked outliers (IQR), fixed inconsistent labels (e.g. `'Y '` vs `'Y'`, `'f'` vs `'F'`)
2. **Feature Engineering** — derived `BMI_Category` (Underweight / Normal / Overweight / Obese) from the raw BMI value
3. **EDA** — univariate, bivariate, and multivariate analysis; HbA1c emerged as the strongest predictor
4. **Preprocessing** — `RobustScaler` for numerical features, `OneHotEncoder` for categorical features, combined via `ColumnTransformer`
5. **Modeling** — compared Logistic Regression, KNN, Decision Tree, Random Forest, and Gradient Boosting via 5-fold cross-validation (accuracy + F1-macro, since the target is imbalanced); tuned the top model with `RandomizedSearchCV`
6. **Deployment** — saved the final pipeline with `joblib` and built a Streamlit app for interactive predictions

## Results

| Model | Test Accuracy | Test F1-Macro |
|---|---|---|
| Logistic Regression | 87.7% | 72.4% |
| KNN | 88.6% | 57.4% |
| Decision Tree | 97.3% | 93.4% |
| Random Forest | 97.4% | 93.7% |
| Gradient Boosting | 98.2% | 95.9% |
| **Tuned Random Forest (final)** | **98%** | **94%** |

## How to Run

1. Clone the repo and install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the notebook `Diabetes_Classification_Full_Project.ipynb` end-to-end to regenerate the cleaned data and trained model files.
3. Launch the app:
   ```bash
   streamlit run Diabetes_Prediction_deployment.py
   ```
4. Open the local URL shown in the terminal (usually `http://localhost:8501`).

## Tech Stack

Python · pandas · scikit-learn · Plotly · Streamlit · joblib

## Dataset

Patient records with demographic info (Gender, Age) and lab results (Urea, Cr, HbA1c, Chol, TG, HDL, LDL, VLDL, BMI) used to determine diabetes diagnosis status.
